
name: Experiments

on:
  push:
    branches: '*'
  pull_request:
    branches: '*'

jobs:
  build:
    runs-on: macos-10.15
    steps:
      - name: Dump GitHub context
        run: echo ${{ toJson(github) }}
